# USDT-M setup scan

Run the daily crypto workflow from a credential-free planning process and focus the analysis on the BTC_USDT and ETH_USDT perpetual sections of the public snapshot. Workflow subagents are read-only, and the only planning handoff is deterministic USDT-M `dry-run` planning after the structured result is persisted by top-level code.

Require the same-week weekly anchor for entries, a complete copied level set, R:R of at least 1.5, and explicit funding/liquidity risks. Missing mode, margin, leverage, account, rule, or reconciliation proof is a blocker.

Never submit from this skill or workflow. Testnet planning and any later submission use a separate human terminal process governed by the gate-trade skill.
